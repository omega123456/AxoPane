[alias]
gm-test-integration = "nextest run --manifest-path src-tauri/Cargo.toml --features test-utils --test ipc_contract_integration --test fs_listing_integration --test fs_mutations_integration --test volumes_enumeration_integration --test persist_storage_integration --test size_manual_integration --test watch_service_integration --test ops_queue_integration --test logging_frontend_integration"
gm-llvm-cov = "llvm-cov nextest --manifest-path src-tauri/Cargo.toml --features test-utils --test ipc_contract_integration --test fs_listing_integration --test fs_mutations_integration --test volumes_enumeration_integration --test persist_storage_integration --test size_manual_integration --test watch_service_integration --test ops_queue_integration --test logging_frontend_integration --fail-under-lines 90 --fail-under-functions 90 --fail-under-regions 80"
