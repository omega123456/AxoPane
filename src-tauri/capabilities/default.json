{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability set for the file explorer core services",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "log:default",
    "updater:default",
    "process:default",
    "process:allow-restart",
    "window-state:default"
  ]
}
